
  // --- native submit (avoids jQuery submit handlers loop) ---
  var formEl = document.getElementById('searchForm');
  if (!formEl) return;
  formEl.setAttribute('action', action);
   console.log('before  formEl.submit();');
  formEl.submit();
  console.log('after  formEl.submit();');